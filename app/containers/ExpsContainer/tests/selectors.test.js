// import { fromJS } from 'immutable';
// import { makeSelectExpsContainerDomain } from '../selectors';

// const selector = makeSelectExpsContainerDomain();

describe('makeSelectExpsContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

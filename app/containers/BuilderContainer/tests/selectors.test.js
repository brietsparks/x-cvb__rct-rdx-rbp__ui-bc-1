// import { fromJS } from 'immutable';
// import { makeSelectBuilderContainerDomain } from '../selectors';

// const selector = makeSelectBuilderContainerDomain();

describe('makeSelectBuilderContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
